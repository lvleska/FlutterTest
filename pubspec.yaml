name: flutter_registration
description: A Flutter registration flow application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP requests
  http: ^1.1.0

  # Image picker for profile photo
  image_picker: ^1.0.4

  # State management (optional - if you want to use Provider or Riverpod)
  # provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # If you have assets, uncomment and add them here:
  # assets:
  #   - images/
  #   - assets/
